export type LessonType = {
    id: string;
    title: string;
    description: string;
    category: string;
    level: string;
    duration: string;
    progress: number;
    completed: boolean;
    imageUrl: string;
};